# Defect Register

Audit date: 2026-08-08. None of these findings was suppressed or converted into a weaker requirement to obtain a passing result.

| ID | Severity | Status | Area | Finding and evidence |
|---|---|---|---|---|
| QA-001 | High | Open | Regression suite | The complete Feature suite is red. Baseline evidence is retained in `artifacts/qa-feature-baseline.out`; final expanded-suite evidence is referenced by `FINAL-TEST-REPORT.md`. |
| QA-002 | Critical → **Narrowed to Medium / doc-currency only** (2026-08-09) | Partially fixed; residue is owner reconciliation required | RBAC | **Original finding (2026-08-08):** `docs/04-roles-permissions.md` defines 27 modules × 10 actions (276 permissions); `CanonicalAuthorizationSeeder` had grown to 28 modules (splitting `purchase_returns` out of `purchase_invoices_supplier_returns`) × 12 actions (adding `submit`/`reject`), reaching 348 permissions, and granted several R-status (owner-approval-required) sensitive actions to roles the doc never names as approvers — plus one genuine misassignment (`pricing_labels.approve`, the matrix's *only* A-status sensitive grant — "Pricing A when configured" — was held by `system-administrator`/`accountant-reviewer` and withheld from `pricing-officer`, the one role the doc actually approves). **2026-08-09 fix (Critical, real defect, not ambiguous):** the seeder synced every role's permissions unconditionally in *all* environments, including a real `APP_ENV=production` run — directly contradicting docs/04 line 12 ("P and R entries are not production grants") with zero owner-decision ambiguity involved. Fixed: `CanonicalAuthorizationSeeder::run()` now applies the full owner-authorized Local/Dev-extended grant set (needed by real, tested approval workflows under DEC-051/052/054/058/059) only outside `production`; a new `productionSafeRolePermissions()` method returns exactly the frozen, doc-approved TSK-008 scope and is what a real Production deploy now receives. Also fixed the `pricing_labels.approve` misassignment (moved from `system-administrator`/`accountant-reviewer` to `pricing-officer`) in both the Production-safe map and the Local/Dev-extended map. `RolePermissionScopeTest::test_no_role_is_granted_an_unapproved_sensitive_permission` and `::test_the_seeded_grants_match_the_documented_current_scope_exactly` were rewritten to assert against `productionSafeRolePermissions()` directly (the map a real Production deploy uses) rather than the `testing`-environment DB state, which intentionally still carries the broader Local/Dev catalog so the many approval-workflow Feature tests can exercise real behavior — both now pass on real evidence, no assertion weakened. `CatalogToInventoryChainTest` was updated to use a second `pricing-officer` user (not `accountant-reviewer`) as the price approver, matching the corrected grant. **Residue still open (Medium, genuinely owner-blocked):** `test_the_canonical_permission_catalog_is_seeded` still fails (348 vs 276) — the `Permission` *catalog* (row definitions, not grants) is not environment-gated, since an ungranted permission row carries no access risk on its own. Whether the owner ratifies a `docs/04` amendment describing 28 modules × 12 actions (the `purchase_returns` split, `submit`/`reject` verbs, and `dispatch`/`receive`/`difference`/`reconcile`/`cost_view`/`approve_over_limit` bespoke actions) to match TSK-014..TSK-022 is a pure documentation-currency question with no Production security exposure now that grants are gated — left open, not resolved by fiat, per the master audit's "REPORT THE MISMATCH" rule. |
| QA-031 | Critical | **Fixed** (2026-08-09) | RBAC / Production seeding | Root cause: see QA-002's 2026-08-09 fix entry — `CanonicalAuthorizationSeeder` granted R-status (owner-approval-required, "not production grants" per docs/04 line 12) sensitive permissions unconditionally, including to a real `APP_ENV=production` seed run, and separately mis-assigned the matrix's one A-status approve grant (`pricing_labels.approve`) to the wrong roles. Fix and regression: as described under QA-002. |
| QA-003 | High | **Fixed** (2026-08-08) | Approval authorization | Root cause: `ExpireApprovalRequest::execute()`'s own docblock documents the intended contract — "System/scheduler calls may run without an authenticated actor. An authenticated invocation must provide its source-specific authorization." No caller is wired yet, so the contract was never exercised. `ApprovalFoundationTest::test_an_expired_request_moves_to_the_expired_state` ran under the class's `setUp()`-level `actingAs()` administrator but never supplied an `$authorize` callback, so production code correctly rejected it — the test violated its own target's documented contract, not a code defect. Fix: split into three tests in `tests/Feature/Audit/ApprovalFoundationTest.php` — the system/scheduler path (`Auth::logout()` then expire, no callback), the authenticated-without-authorization rejection path (now an explicit positive assertion of the guard), and the authenticated-with-authorization success path. No production code changed. Regression: `ApprovalFoundationTest::test_an_expired_request_moves_to_the_expired_state_via_the_system_scheduler_path`, `::test_an_authenticated_expiry_action_is_rejected_without_explicit_authorization`, `::test_an_authenticated_expiry_action_succeeds_with_explicit_authorization`. |
| QA-004 | Medium | **Fixed** (2026-08-08) | Regression maintenance | Four stale absence assertions were replaced with behavioral coverage of the now-implemented features, none weakened: (1) `tests/Feature/Catalog/CatalogImplementationAbsenceTest.php` deleted outright — TSK-010 (products/categories/brands/suppliers/barcodes) is fully superseded by the existing `CatalogMasterBehaviorTest`, `ImportRuntimeCompatibilityTest`, and `ProductMediaAuthorizationTest`, so nothing remained to preserve. (2) `ApprovalFoundationTest::test_the_approval_foundation_is_not_wired_to_any_current_source` replaced with `::test_the_approval_foundation_is_now_wired_through_pricing_proposal_submission`, which confirms `SubmitPriceProposalAction` requests approval end-to-end and asserts the resulting `ApprovalRecord`'s `source_type`/`source_id`/`requested_action`/pending state (full lifecycle already covered by `PriceProposalIntegrityTest`). (3) `SharedUiFoundationTest::test_no_dedicated_print_route_is_implemented_yet` replaced with `::test_dedicated_print_routes_are_implemented_and_permission_gated`, which asserts `sales.print`, `purchasing.returns.print`, `purchasing.invoices.print`, and `purchasing.orders.print` are registered and each route's source file references its `.print` ability (mixed `can:` middleware / `Gate::authorize()` patterns, both verified). |
| QA-005 | High | Open | Test infrastructure | Required `scripts/run-tests.sh`, Dusk/browser suite, Backstop, axe, k6, Enlightn, smoke/load scripts, and the documented complete orchestrator are absent. |
| QA-006 | High | Open | Imports/dependencies | `composer.lock` requires OpenSpout 4.32 but installed vendor metadata/package files are absent. PHPStan reports missing `ReaderFactory` and `FormulaCell`; the implementation also uses the removed v3 factory-style reader API. Product and purchase-invoice spreadsheet import cannot be verified or considered runnable. |
| QA-007 | Medium | Open | Code style | `pint --parallel --test` fails on five production files: `DownloadProductImportErrorsAction.php`, `StageProductImportAction.php`, `ProductImportBatch.php`, `ProductImportRow.php`, and `routes/catalog.php`. |
| QA-008 | High | Open | Frontend dependency security | `npm audit --json` reports one high-severity `nanoid` advisory (`GHSA-2v37-7h3g-55p8`), with a fix available. |
| QA-009 | Medium | Open | Local database integrity | The default local SQLite database lacks `stock_movements`; `inventory:rebuild-balances` fails there. A clean temporary database migrates/seeds and reconciles successfully, proving local environment drift. |
| QA-010 | High | Open | Scheduler/operations | `php artisan schedule:list` reports no scheduled tasks. Required expiry, alert, reconciliation, and operational jobs have no verified schedule. |
| QA-011 | High | Open | Reconciliation | No project-wide `recon:check` exists. Only inventory balance rebuild/dry-run is present; wallets, gift cards, shifts, party balances, and financial reconciliation are uncovered. |
| QA-012 | Critical | Open | Backup/restore | No implemented backup/restore command, schedule, runbook execution, or restore-drill evidence exists. Production RPO/RTO and ownership are unresolved. |
| QA-013 | Critical | Open | POS/financial posting | Retail sale posting records total/paid values and stock, but there is no complete payment/evidence/drawer/customer posting chain required by POS-02/POS-03. Financial completeness is not demonstrated. |
| QA-014 | Critical | **Partial** (2026-08-09) | Concurrency | Originally: retail idempotency lookup outside the transaction, and no environment to prove production row-lock semantics (SQLite only). Both are now partially addressed: the idempotency-outside-transaction race is fixed for `PostInventoryMovement` and `RetailSaleAction` (see QA-037/QA-038 below), and true concurrent row-lock safety is now demonstrated with a real MariaDB instance and genuine racing OS processes for 4 of the highest-risk lockable paths — stock balance posting, PO number allocation, price-approval active-version uniqueness, and POS-vs-limited-stock (`tests/Concurrency/`, `CONCURRENCY-SCENARIOS.md`). Still open: the remaining ~19 `CONC-` scenarios (transfer dispatch/receipt races, cashier shift/drawer races, offline queue convergence, and everything gated on an unimplemented module) are untested or `BLOCKED_NOT_IMPLEMENTED`; k6/load-level and browser-level concurrent-user proof are absent. Not closed — `PRODUCTION-RELEASE-GATE.md` gate #10 is `PARTIAL`, not `PASS`. |
| QA-015 | Critical | **Fixed** (2026-08-08) | Idempotency | Root cause: `PostInventoryMovement::execute()`, `RetailSaleAction::create()`, and `CreatePurchaseReturnDraftAction::execute()` all looked up the existing record by `idempotency_key` and returned it unconditionally, without comparing the replay's payload to the original. Fix: each action now compares the full request signature against the stored record before returning it — `PostInventoryMovement` compares product/store/movement_type/quantity/unit_cost/source_type/source_id/source_line_id; `RetailSaleAction` compares store/cashier/suspend-flag and each line's product+quantity via a new `linesMatch()` helper; `CreatePurchaseReturnDraftAction` compares invoice/reason and each line's source-line-id+quantity via a new `linesMatch()` helper. An exact-payload replay still returns the original record (verified by the pre-existing `test_identical_idempotency_replay_returns_the_original_movement_once` and the sale/return exact-replay paths); a conflicting-payload replay now throws `InvalidArgumentException`. No stock/financial side effects are duplicated. Regression: `InventoryMovementIntegrityTest::test_idempotency_key_replay_with_conflicting_payload_is_rejected_without_duplicate_effect`, `RetailSaleIntegrityTest::test_approved_sale_rejects_a_conflicting_idempotency_replay_without_duplicate_effect`, `PurchasingLifecycleIntegrityTest::test_supplier_return_rejects_reuse_of_an_idempotency_key_with_a_conflicting_payload`. |
| QA-016 | High | Open | Wallets | Product and Party Wallet tables/models are separated, but full source-linked customer ledger mutation, balance, settlement, visibility, and reporting behavior is not implemented. Storage separation alone does not satisfy CUS-02. |
| QA-017 | Critical | Open / narrowed 2026-08-09 | Functional scope | Wallets, gift cards, returns, party booking/payments/operations/assets, quotations, reports/exports/alerts remain readiness/foundation gaps. TSK-027 customer master and retail-loyalty mutations are now implemented and have separate closure evidence; this defect remains open for the other functional areas. |
| QA-018 | Critical | Open | Offline POS | Restricted offline transaction queue, trusted payload, replay protection, synchronization, conflict resolution, and reconciliation are absent. |
| QA-019 | Critical | Open | Cash shifts | Full transaction-linked shifts, cash movements, blind close, post-submit variance, reviewer workflow, and immutable close documents are absent. A readiness boundary is not the required workflow. |
| QA-020 | High | Open / environment partly blocking | Outputs/devices | Required print formats, barcode queues, receipt printers, scanners, cameras, and device matrices lack executable browser/device evidence. |
| QA-021 | Medium | Open | API/direct access | The application intentionally uses Laravel web routes rather than ordinary-screen APIs. Direct-route authorization has partial tests, but no complete route/action matrix for every permission and scope exists. |
| QA-022 | Critical | Open / human evidence required | Acceptance | UAT, device acceptance, DR drill, client sign-off, controlled cutover, rollback rehearsal, and production security review remain outstanding. |
| QA-023 | Critical | Open / environment | Production platform | Production database, cache, queues, scheduler, storage, monitoring, secrets, deployment topology, and support ownership are undecided or unavailable. |
| QA-024 | High | Open | Performance | A high-volume fixture generator exists, but no k6/load suite, latency thresholds execution, sustained concurrency test, or browser performance evidence exists. |
| QA-025 | Low | Open | Unit testability | Calculation/policy classes call Laravel translation helpers on negative paths, so their nominal unit tests require framework bootstrapping. |
| QA-026 | Medium | Open | QA documentation | Testing documents refer to `docs/testing/**` although the supplied suite is `testing/**`, reference a different PRD filename, and require absent scripts/tools. |
| QA-027 | High | **Fixed** (2026-08-08) | Inventory validation | Root cause: `PostInventoryMovement::execute()` had no domain-boundary guard for `Product.fractional_quantity`; the flag existed on the model and in catalog import/save actions but was never consulted before posting a stock movement. Fix: `execute()` now loads the product's `fractional_quantity` flag, computes the quantity's fractional remainder via `bcmod($quantity, '1', 6)`, and throws `InvalidArgumentException` when a non-zero fractional quantity is posted against a product with `fractional_quantity = false`. Regression: `InventoryWorkflowIntegrityTest::test_fractional_quantity_is_rejected_for_a_product_without_fractional_configuration` (also verified the pre-existing fractional-allowed and whole-number paths still pass). |
| QA-028 | Critical | **Fixed** (2026-08-08) | IDOR / horizontal escalation | Root cause: `routes/purchasing.php`'s `purchasing/invoices/{invoice}/print` and `purchasing/orders/{order}/print` routes checked only the `.print` ability (`Gate::authorize(...)`) with no store-scope check, unlike their sibling `purchasing/returns/{return}/print` (which already checks `Store::visibleTo($user)->whereKey($return->store_id)->exists()`) and `sales/{sale}/print`. Any actor holding `purchase_invoices_supplier_returns.print` or `purchase_orders.print` — e.g. a Purchasing Officer or non-super-admin System Administrator scoped to one store — could view another store's purchase invoice or order (supplier, cost, and pricing data) by incrementing the URL ID. Violates docs/04's "Receiving-store scope" / "Delegated supplier/store" boundary for these modules. Fix: both routes now run the same `Store::visibleTo($user)->whereKey(...)->exists()` (or `is_super_admin`) check as their siblings before rendering; a store-less (company-wide) purchase order is deliberately exempted since it has no store to scope against. Regression: `tests/Feature/Security/CrossStoreIdorTest.php` — `test_a_scoped_administrator_cannot_print_a_purchase_order_from_another_store`, `test_a_company_wide_purchase_order_with_no_store_is_printable_by_any_permission_holder`, `test_a_scoped_administrator_cannot_print_a_purchase_invoice_from_another_store`, plus same-store/cross-store confirmation for `sales.show`/`sales.print` and `purchasing.returns.show`/`purchasing.returns.print` (the latter two already had correct code but no live regression coverage before this pass). |
| QA-029 | Medium | **Closed — gap filled** (2026-08-08) | Test coverage / IDOR | The shared `AssertInventoryStoreScope` helper (relied on by 8 inventory mutation actions: transfer approve/dispatch/receive/difference-resolve, adjustment submit/approve, count submit/reconcile) had regression coverage through only one call site (`ApproveStockTransferAction`, via `InventoryWorkflowIntegrityTest`). Added `tests/Feature/Inventory/InventoryStoreScopeGuardTest.php`: direct coverage of the helper (no user, super-admin bypass, visible-store pass, foreign-store reject, and the `transfer()` source/destination-independent checks) plus a second real call-site regression via `SubmitInventoryAdjustmentAction`. No code defect found — the helper and its wiring were already correct; this closes a coverage gap, not a behavior defect. |
| QA-030 | Medium | **Fixed** (2026-08-08) | Audit trail | Found while building the CATALOG→PRICING→POS→INVENTORY chain E2E test: `RetailSaleAction::finalize()` posted the stock movement(s) for an approved sale but never called `RecordAuditEvent`, unlike every other financial/inventory-mutating action in the codebase (purchase invoice/return approval, transfers, adjustments, stock counts, price proposal submit/approve all record an audit row). This left the single highest-frequency financial mutation in the system — a completed POS sale — with no audit trail, contradicting docs/04's Permission Evaluation Order step 8 ("The action and reason are audited where required") and E2E-17's explicit "Sale/payment/stock/receipt audit linked" expectation. Fix: `finalize()` now records a `retail` / `finalize_sale` audit event with before/after status, branch/store, line count, cashier, and a `stock_posted` flag, following the exact pattern already used by every sibling action. Regression: `CatalogToInventoryChainTest` asserts the `finalize_sale` audit row exists and links to the sale; full `RetailSaleIntegrityTest`/`RetailSuspendedAndBarcodeTest` suites re-verified with no behavior change beyond the new audit row. |
| QA-032 | Medium | **Fixed** (2026-08-09) | Accessibility (WCAG 1.3.1) | Found via `testing/e2e/critical-accessibility-rtl-mobile.spec.js` (axe-core, real browser). `resources/views/dashboard.blade.php`'s "Foundation status" list used `<dl><div class="dashboard-status-row"><div><dt>…</dt><dd>…</dd></div><span>…</span></div></dl>` — two defects layered: the `<dt>`/`<dd>` pair was nested two levels inside `<dl>` (only one wrapping `<div>` is permitted per the HTML5 `<dl>` content model), and even after flattening that, the row's status `<span>` sibling inside the same `<div>` still violated the model, which requires each `<div>` child of `<dl>` to contain *only* dt/dd groups. Root cause: this data is a three-part label/description/status row, not a pure term/definition pair — `<dl>` was the wrong element. Fix: replaced with a plain `<ul>`/`<li>` list (label and description as `<p>` tags, status as a sibling `<span>`), which carries list semantics without an ill-fitting content-model constraint. Regression: `critical-accessibility-rtl-mobile.spec.js` — `the administrator dashboard has no critical/serious violations` and the RTL variant, both axe-clean. |
| QA-033 | Medium | **Fixed** (2026-08-09) | Accessibility (WCAG 1.3.1) | `resources/views/catalog/suppliers.blade.php`'s supplier-detail profile fields used `<dt>`/`<dd>` pairs with no `<dl>` ancestor at all (a plain `<div class="grid gap-4 sm:grid-cols-2">` wrapper) — found by code inspection while fixing QA-032's same defect class, not yet covered by a direct axe scan of this page. Fix: changed the wrapping element from `<div>` to `<dl>`; the existing per-field `<div class="catalog-detail-field">` wrappers already correctly contain only a `<dt>`+`<dd>` pair each, so no other structural change was needed. |
| QA-034 | Medium | **Fixed** (2026-08-09) | Accessibility (WCAG 1.4.3 color contrast) | Found via `critical-accessibility-rtl-mobile.spec.js`: the sidebar's "Platform" navigation group heading rendered at a 2.48:1 contrast ratio (`#a3a3a3` on `#fafefe`; 4.5:1 required for normal text). Root cause: `resources/css/app.css` already had a rule intending to fix exactly this (`.app-sidebar [data-flux-sidebar-group-heading] { @apply ... text-text-muted; }`), but Flux UI's non-expandable `<flux:sidebar.group>` heading (`vendor/livewire/flux/stubs/resources/views/flux/sidebar/group.blade.php` line 94) never actually renders a `data-flux-sidebar-group-heading` attribute — the selector matched nothing, so the intended fix silently never applied, and the heading fell through to Flux's own default `text-zinc-400`. Fix: added a rule targeting the actual rendered node by its real class list (`[data-flux-sidebar-group] > div > .text-zinc-400.font-medium.leading-none`), with `!important` — required because this rule lives in Tailwind's `@layer components` while Flux's utility lives in `@layer utilities`, and CSS Cascade Layers give layer order absolute priority over selector specificity, so a plain higher-specificity selector still loses. Regression: `critical-accessibility-rtl-mobile.spec.js` — dashboard axe scans (desktop and RTL) now clean of this violation. |
| QA-035 | Medium | **Fixed** (2026-08-09) | Mobile responsiveness | Found via `critical-accessibility-rtl-mobile.spec.js` at a 390px viewport: the POS screen (`resources/views/pages/pos/index.blade.php`) overflowed the page horizontally by 180px. Root cause: the Cart table (`min-w-[520px]`, correctly wrapped in its own `overflow-x-auto`) sits inside a CSS Grid layout (`grid lg:grid-cols-[1.35fr_0.9fr]`); the enclosing `<section>` grid item had no explicit `min-width`, so its default automatic minimum size (its content's min-content size) was pulled up from the table's 520px minimum, forcing the whole grid — and therefore the page — wider than the viewport, even though the table's own overflow was correctly contained. Fix: added `min-w-0` to the `<section>` grid item, the standard fix for this well-known CSS Grid/Flexbox sizing behavior. Regression: `critical-accessibility-rtl-mobile.spec.js` — `the POS screen has no horizontal overflow ... at 390px`. |
| QA-036 | Low | **Fixed** (2026-08-09) | Accessibility (WCAG 2.1.1 keyboard access) | Surfaced immediately after fixing QA-035: once the Cart's `overflow-x-auto` wrapper became the page's only horizontally-scrollable region (rather than the whole page scrolling), axe-core correctly flagged it as a scrollable region with no keyboard access (no `tabindex`, so keyboard-only users cannot scroll it, e.g. in Safari). Fix: added `tabindex="0" role="region" aria-label="{{ __('Cart') }}"` to the wrapper. Regression: same `critical-accessibility-rtl-mobile.spec.js` POS/390px test, now clean. |
| QA-037 | Critical | **Fixed** (2026-08-09) | Inventory idempotency (concurrency) | Found only by racing two genuine OS processes against real MariaDB (SQLite cannot exercise this): `PostInventoryMovement::execute()` (`app/Modules/Inventory/Actions/PostInventoryMovement.php`) checked `StockMovement::where('idempotency_key', ...)->first()` for an existing row, then inserted a new one, inside one `DB::transaction()` — but the check-then-insert is not itself lockable, so two concurrent callers submitting the identical duplicate request (e.g. a flaky client retry firing before the first response returns) can both pass the check before either commits. `stock_movements.idempotency_key` is a unique DB column, so the second insert does not corrupt data, but it threw an unhandled `Illuminate\Database\UniqueConstraintViolationException` — surfaced to the caller as a raw 500 instead of the intended graceful idempotent replay, violating the idempotency contract the rest of the code already implements for the non-concurrent case. Fix: catch the specific unique-constraint violation, re-fetch the row the other transaction committed, and return it through the existing replay-safety check (same-payload → return existing row; different payload → the existing, unchanged `InvalidArgumentException`). Regression: `tests/Concurrency/StockBalanceConcurrencyTest.php::test_two_concurrent_identical_idempotency_key_submissions_collapse_to_one_movement` (real MariaDB, two real OS processes racing the same idempotency key) — both processes now return the same movement row, exactly one row exists, no unhandled exception. A second test in the same file (`test_two_concurrent_distinct_movements_do_not_lose_an_update`) positively proves the pre-existing `StockBalance` `lockForUpdate()` correctly serializes two *different* concurrent movements with no lost update — that part was never broken. |
| QA-038 | Critical | **Fixed** (2026-08-09) | POS sale idempotency (concurrency) | Same root cause as QA-037, in `RetailSaleAction::create()` (`app/Modules/Retail/Actions/RetailSaleAction.php`): the idempotency lookup (`Sale::where('idempotency_key', ...)->first()`) ran entirely *before* `DB::transaction()` even opened, widening the same TOCTOU race. `sales.idempotency_key` is unique, so a duplicate concurrent checkout submission (e.g. a cashier's double-tap on a slow connection) again could not corrupt data but did throw an unhandled `UniqueConstraintViolationException` instead of replaying the original sale. Fix: wrap shift-opening and sale creation in a try/catch for the unique-constraint violation, re-fetch, and replay through the existing line-match safety check. Regression: `tests/Concurrency/RetailSaleConcurrencyTest.php::test_two_concurrent_identical_idempotency_key_sale_submissions_collapse_to_one_sale` — both racers resolve to the same sale, exactly one `Sale` row and one stock deduction. A second test in the same file (`test_two_concurrent_sales_against_limited_stock_never_oversell`) positively proves `RetailSaleAction::finalize()`'s pre-existing `StockBalance` `lockForUpdate()` correctly prevents oversell when two *different* concurrent sales compete for the same limited stock (one succeeds, one fails cleanly with "Insufficient stock", on-hand never goes negative) — that part was never broken either. |
| QA-039 | Medium | **Closed — gap filled** (2026-08-09) | Test coverage / failure recovery | Three scenarios (FAIL-INV-003 transfer receipt, FAIL-INV-004 count reconciliation, FAIL-POS-003 suspended-sale resume) had only ever been tested for the first line of a multi-line `DB::transaction()` failing (trivial — nothing yet to roll back) or every line succeeding — never "a later line fails after an earlier line's write already applied," the actual atomicity claim. Added real fault-injection tests (a genuine `PostInventoryMovement` fractional-quantity rejection on the second of two lines, not a mock) proving the first line's already-applied write rolls back together with the second line's failure in all three actions, including the `InventoryAdjustment` header row for count reconciliation. A positive-control test and an explicit line-iteration-order assertion were added to the transfer-receipt case (and an order-independent `document_sequences`-absence assertion to the POS case) after review found the original assertions could not distinguish "rolled back" from "the failing line ran first and the succeeding line was never reached." All three actions' existing transaction wrapping was already correct — no defect found, only previously-unproven assumptions converted into real evidence. Same disposition pattern as QA-029. See `tests/Feature/Inventory/InventoryFaultInjectionAtomicityTest.php` and `tests/Feature/Retail/RetailSaleIntegrityTest.php`. |
| QA-040 | Medium | **Fixed** (2026-08-09) | Platform UI (branch/store mapping history) | Found only by running the full suite against real MariaDB (production-like regression pass) — SQLite masked this entirely. `openHistoryModal()` in `resources/views/platform/admin/branches.blade.php` ordered the mapping-history list with `->sortByDesc('created_at')`, a Laravel Collection sort. MySQL's default `timestamp` column precision is whole seconds (SQLite preserves microseconds), so two mappings created within the same real second — an entirely plausible admin action — tie on `created_at`, and the sort's tie-break silently falls back to whatever order Eloquent loaded the rows in (effectively insertion order), showing the OLDER mapping first instead of the newer one. An admin reviewing mapping history could misread which store is currently active. Fix: sort by `id` (the table's auto-increment primary key) instead — a monotonic, unambiguous proxy for creation order regardless of timestamp precision. Regression: `tests/Feature/Platform/BranchStoreMappingTest.php::test_the_branch_screen_records_mapping_history_for_review`, verified failing before the fix and passing after under real MariaDB (`phpunit.prodlike.xml`); unaffected (already passing, both before and after the fix) under the default SQLite suite — which is precisely why this defect went undetected until this pass ran against a real MySQL-family engine. Swept the rest of `app/` and `resources/` for the same tie-prone pattern (`sortBy`/`sortByDesc`/`orderBy`/`->latest(`/`->oldest(` on a `*_at` column): one other instance found, `RetailSaleAction::openShift()`'s `->latest('opened_at')` — not fixed, because no production code path creates a `PosShift` yet (shift-opening is test/seeder-only, consistent with `CONC-CSH-001: BLOCKED_NOT_IMPLEMENTED`), so the tie is currently unreachable; flagged to revisit once shift-opening ships. |
| QA-041 | — | **Test defect, fixed** (2026-08-09, no production code change) | Test coverage (audit backfill) | Also found only under real MariaDB. `AuditBackfillTest::test_the_legacy_source_key_is_unique` and `test_legacy_values_are_redacted_during_the_backfill` both hardcoded the legacy row's expected primary key as `settings_audit_logs:1`. Root cause: MySQL/InnoDB's `AUTO_INCREMENT` counter is not transactional — it does not reset when `RefreshDatabase` rolls back an earlier test's inserts in the same file — so by the time these two tests run, the real inserted `id` has already drifted past `1` (confirmed live: `SHOW TABLE STATUS` reported `Auto_increment=9` with 0 actual rows in the table). SQLite happens to reset on rollback, which is the only reason `id === 1` ever held. Effect: the "unique" test's duplicate-key assertion collided with nothing (no exception thrown, since no row actually had that key) and the redaction test's lookup query returned no row (`Attempt to read property "before_values" on null`). `BackfillLegacySettingsAuditLogs` itself was correct throughout — it always keyed by the row's real `id`, exactly as intended. Fix: both tests now read the actual inserted `id` via `DB::table('settings_audit_logs')->value('id')` instead of assuming `1`. Same disposition pattern as QA-003/QA-004 (a stale test assumption, not a production defect). Regression: both tests verified failing before the fix and passing after under `phpunit.prodlike.xml`; unaffected (already passing) under the default SQLite suite. |
| QA-042 | Critical | **Fixed** (2026-08-09) | Database migrations (deployment rollback) | Found while executing the Migration/Rollback master-priority item: `database/migrations/2026_08_06_000026_extend_purchase_invoices_for_lifecycle.php`'s `down()` called `$table->dropForeign(['created_by', 'updated_by', 'submitted_by', 'rejected_by', 'cancelled_by'])` — a single call with 5 column names in one array. Laravel's `Blueprint::dropIndexCommand()`/`createIndexName()` (confirmed by reading `vendor/laravel/framework/src/Illuminate/Database/Schema/Blueprint.php`) treats an array argument as the column list for ONE composite constraint name, not as 5 independent single-column drops — it tries to drop a constraint literally named `purchase_invoices_created_by_updated_by_submitted_by_rejected_by_cancelled_by_foreign`, which never existed (the 5 real constraints were each created separately via individual `$table->foreignId(...)->constrained(...)` calls in `up()`, each with its own conventional name). Effect: running `php artisan migrate:rollback` on this migration crashes outright with a database error instead of cleanly reversing it — a real production deployment needing to roll back this release would fail here. Verified empirically, not just from source: reproduced the exact crash via a full `migrate` + `migrate:rollback` cycle against both an isolated SQLite file (`SQLSTATE[HY000]: ... unknown column "created_by" in foreign key definition`) and a dedicated real MariaDB database. Fix: replaced the single call with 5 separate `dropForeign(['column'])` calls, one per column. Regression: reran the full rollback cycle against both engines after the fix — clean, zero errors — and added permanent coverage, `tests/Feature/Platform/MigrationRollbackIntegrityTest.php`, which runs a full `migrate`+`migrate:rollback` cycle on its own dedicated, isolated SQLite connection and asserts a clean round trip; verified this test genuinely detects the regression by temporarily reverting the fix and confirming the test fails with the same error, then restoring the fix and confirming it passes. |
| QA-043 | Critical | **Fixed** (2026-08-09) | Database migrations (deployment rollback) | Found by the same Migration/Rollback verification pass as QA-042, using the same full `migrate`+`migrate:rollback` round trip. Two further migrations dropped a column without first dropping the single-column index/unique constraint built on it — the same underlying mistake as QA-042 (a `down()` that doesn't fully reverse everything `up()` created), just via an omitted `dropIndex`/`dropUnique` call rather than a malformed one: (1) `database/migrations/2026_08_04_000018_extend_products_for_product_cards.php` — `up()` gives `product_type`, `colour`, `size`, and `character` each their own single-column index via `->index()`, but `down()` only dropped the 3 *composite* `[column, status]` indexes before calling `dropColumn(...)` on all four, plus more — reproduced crash under SQLite: `error in index products_product_type_index after drop column: no such column "product_type"`. Fixed by adding explicit `dropIndex(['product_type'])`, `dropIndex(['colour'])`, `dropIndex(['size'])`, `dropIndex(['character'])` before the `dropColumn` call. (2) `database/migrations/2026_08_03_000000_add_username_and_super_admin_to_users_table.php` — `up()` adds `username` with `->unique()`; `down()` dropped the column directly with no `dropUnique` first — reproduced crash: `error in index users_username_unique after drop column: no such column "username"`. Fixed by adding `dropUnique(['username'])` before `dropColumn`. Both fixes verified the same way as QA-042: reproduced failing before the fix (exact SQL errors above) and passing after, via a full 41-migration `migrate`+`migrate:rollback` round trip against both an isolated SQLite file and a dedicated real MariaDB database; both are covered by the same new `MigrationRollbackIntegrityTest.php` regression test (which rolls back the entire migration set in one pass, not just these two files individually). |

| QA-044 | High | **Fixed** (2026-08-10) | Loyalty approval workflow | `loyalty_adjustments` created canonical `ApprovalRecord` rows but the shared Approval Inbox did not dispatch their approve/reject transitions. Added source-owned approval/rejection actions, mandatory rejection reason, requester separation, immutable status transition, audit, direct route, and regression coverage on SQLite and MariaDB. |

## Disposition

QA-003, QA-004, QA-015, QA-027, QA-028, and QA-030 are fixed and regression-verified as of 2026-08-08; QA-031 through QA-036 are fixed and QA-002 is narrowed (Critical → Medium, doc-currency only) as of 2026-08-09; QA-037 and QA-038 (concurrency automation pass, same day) are fixed and regression-verified against real MariaDB, and QA-014 is narrowed from Open to Partial on the strength of that same evidence (see `.ai/TEST_RESULTS.md` for the full re-run). QA-040 and QA-041 (production-like regression pass, same day) were found ONLY by running the full suite against real MariaDB and are fixed and regression-verified. QA-042 and QA-043 (migration/rollback pass, same day) were found by actually executing `php artisan migrate:rollback` end to end for the first time this audit (previously `BLOCKED_BY_ENVIRONMENT`/untested) and are fixed and regression-verified against both SQLite and real MariaDB, with new permanent test coverage. QA-044 (TSK-027 loyalty approval dispatch) is fixed and regression-verified against SQLite and MariaDB. QA-029 and QA-039 are closed coverage gaps (no code change — previously-unproven transaction-atomicity/helper-coverage assumptions converted into real, executed evidence). QA-002's residue (the 348-vs-276 permission catalog count) remains open pending an explicit owner decision on the docs/04 amendment — see its row above; it is no longer a Production security exposure. All other Critical/High defects (QA-005 through QA-024, excluding those just closed or narrowed) remain open. Production acceptance is therefore still prohibited even where a Local/Dev readiness screen or focused test passes.

**Tooling correction (2026-08-09, same day):** the production-like regression pass's original "crashed on PHP's default 128M `memory_limit`, fixed by re-running with `-d memory_limit=512M`" note (see `.ai/TEST_RESULTS.md`) was incomplete — that CLI flag never actually reaches the real PHPUnit process (`php artisan test` shells out to a child process via `nunomaduro/collision` that ignores flags passed to the outer process), so that clean run was a coincidence of test ordering, not a real fix. A later re-run crashed for real on the same limit. The genuine, verified fix is `<ini name="memory_limit" value="512M"/>` added to `phpunit.prodlike.xml`, which PHPUnit applies inside the actual child process. Not a defect in application code — a test-tooling correction only. See `.ai/TEST_RESULTS.md`'s "Production-like Regression — Correction Pass" entry for full detail.
