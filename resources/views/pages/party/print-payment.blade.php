<!doctype html><html lang="{{ str_replace('_','-',app()->getLocale()) }}" dir="{{ app()->getLocale()==='ar' ? 'rtl' : 'ltr' }}"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><title>{{ $payment->receipt_label }}</title>@vite(['resources/css/app.css'])<style>@page{size:A4;margin:14mm}@media print{.no-print{display:none!important}}</style></head><body class="bg-white text-zinc-900"><main class="mx-auto max-w-3xl p-6"><div class="no-print mb-5 flex justify-end"><button type="button" onclick="window.print()" class="rounded-lg bg-zinc-900 px-4 py-2 text-sm font-semibold text-white">{{ __('Print') }}</button></div><header class="flex items-start justify-between gap-4 border-b border-zinc-300 pb-5"><div><h1 class="text-2xl font-bold">TOY & JOY</h1><p class="mt-1">{{ __('Party payment receipt') }}</p><p class="text-sm">{{ $payment->invoice->booking->store->name_en }}</p></div><div class="text-end"><div class="font-mono text-xl font-bold">{{ $payment->receipt_number }}</div><div class="text-sm">{{ $payment->created_at?->format('Y-m-d H:i') }}</div></div></header><div class="mt-6 rounded-xl border border-cyan-300 bg-cyan-50 p-4 font-semibold">{{ $payment->receipt_label }}</div><dl class="mt-6 grid gap-4 sm:grid-cols-2 text-sm"><div><dt class="text-zinc-500">{{ __('Customer') }}</dt><dd class="font-semibold">{{ $payment->invoice->booking->customer->name_en }}</dd></div><div><dt class="text-zinc-500">{{ __('Party booking') }}</dt><dd class="font-mono font-semibold">{{ $payment->invoice->booking->booking_number }}</dd></div><div><dt class="text-zinc-500">{{ __('Invoice') }}</dt><dd class="font-mono font-semibold">{{ $payment->invoice->invoice_number }}</dd></div><div><dt class="text-zinc-500">{{ __('Payment method') }}</dt><dd class="font-semibold">{{ $payment->method_code }}</dd></div></dl><div class="mt-8 flex justify-between border-t border-zinc-300 pt-4 text-xl font-bold"><span>{{ __('Amount received') }}</span><span>{{ number_format((float)$payment->amount,2) }} {{ $payment->invoice->currency_code }}</span></div><p class="mt-8 text-sm text-zinc-600">{{ __('This is a Party payment on account. Party Wallet and Product Wallet are separate ledgers; this receipt does not post to Product Wallet.') }}</p></main><script>window.addEventListener('load',()=>window.print())</script></body></html>
