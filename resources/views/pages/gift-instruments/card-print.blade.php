<!doctype html>
<html lang="{{ str_replace('_', '-', app()->getLocale()) }}" dir="{{ in_array(app()->getLocale(), ['ar', 'ar_SA'], true) ? 'rtl' : 'ltr' }}">
<head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><title>{{ __('Gift Card') }} {{ $card->identifier }}</title><style>body{font-family:Arial,sans-serif;margin:24px;color:#18181b}.card{max-width:420px;border:1px solid #d4d4d8;border-radius:16px;padding:24px}.muted{color:#71717a}.value{font-size:28px;font-weight:700;margin:18px 0}dt{font-weight:700;margin-top:12px}dd{margin:2px 0 0}.print{margin-top:24px}@media print{.print{display:none}body{margin:0}.card{border:0}}</style></head>
<body><main class="card"><p class="muted">{{ $event->is_reprint ? __('Gift Card reprint') : __('Gift Card') }}</p><h1>{{ $card->identifier }}</h1><p class="value">{{ number_format((float) $card->balance, 2) }} {{ $card->currency_code }}</p><dl><dt>{{ __('Status') }}</dt><dd>{{ __($card->status) }}</dd>@if($card->valid_until)<dt>{{ __('Valid until') }}</dt><dd>{{ $card->valid_until->toDateString() }}</dd>@endif</dl><p class="muted">{{ __('Keep this identifier safe. Balance is maintained by the Gift Card ledger.') }}</p><button class="print" type="button" onclick="window.print()">{{ __('Print') }}</button></main></body>
</html>
