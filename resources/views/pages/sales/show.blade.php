<x-layouts::app :title="__('Sale :number', ['number' => $sale->document_number])">
    <div class="mx-auto w-full max-w-5xl space-y-4 p-4">
        <div class="flex flex-wrap items-start justify-between gap-3" data-guide="sale-detail-header"><div><flux:heading size="xl">{{ __('Sale') }} {{ $sale->document_number }}</flux:heading><flux:text class="mt-1">{{ __('Local/Dev retail sale detail.') }}</flux:text></div><div class="flex gap-2"><flux:button href="{{ route('sales.print', $sale) }}" target="_blank" variant="subtle" icon="printer" data-guide="sale-detail-print-action">{{ __('Print') }}</flux:button><flux:button href="{{ route('sales.index') }}" variant="subtle">{{ __('Back') }}</flux:button></div></div>
        <div class="grid gap-4 sm:grid-cols-3" data-guide="sale-detail-meta"><div class="rounded-xl border border-zinc-200 bg-white p-4 dark:border-zinc-800 dark:bg-zinc-900"><div class="text-xs text-zinc-500">{{ __('Store') }}</div><div class="mt-1 font-semibold">{{ app()->getLocale() === 'ar' ? $sale->store->name_ar : $sale->store->name_en }}</div></div><div class="rounded-xl border border-zinc-200 bg-white p-4 dark:border-zinc-800 dark:bg-zinc-900"><div class="text-xs text-zinc-500">{{ __('Cashier') }}</div><div class="mt-1 font-semibold">{{ $sale->cashier->name }}</div></div><div class="rounded-xl border border-zinc-200 bg-white p-4 dark:border-zinc-800 dark:bg-zinc-900"><div class="text-xs text-zinc-500">{{ __('Status') }}</div><div class="mt-1 font-semibold text-emerald-700">{{ __('Approved') }}</div></div></div>
        <div class="overflow-x-auto rounded-xl border border-zinc-200 bg-white shadow-xs dark:border-zinc-800 dark:bg-zinc-900"><table class="w-full min-w-[640px] text-sm"><thead class="border-b border-zinc-200 text-xs text-zinc-500 dark:border-zinc-800" data-guide="sale-detail-lines-heading"><tr><th class="p-4 text-start">{{ __('Product') }}</th><th class="p-4 text-end">{{ __('Quantity') }}</th><th class="p-4 text-end">{{ __('Unit price') }}</th><th class="p-4 text-end">{{ __('Amount') }}</th></tr></thead><tbody>@foreach ($sale->lines as $line)<tr class="border-b border-zinc-100 last:border-0 dark:border-zinc-800"><td class="p-4"><div class="font-medium">{{ app()->getLocale() === 'ar' ? $line->name_ar : $line->name_en }}</div><div class="text-xs text-zinc-500">{{ $line->item_code }}</div></td><td class="p-4 text-end">{{ $line->quantity }}</td><td class="p-4 text-end">{{ number_format((float) $line->unit_price, 2) }}</td><td class="p-4 text-end font-semibold">{{ number_format((float) $line->net_amount, 2) }}</td></tr>@endforeach</tbody><tfoot class="border-t border-zinc-200 dark:border-zinc-700"><tr><td colspan="3" class="p-4 text-end font-bold">{{ __('Total') }}</td><td class="p-4 text-end text-lg font-bold">{{ number_format((float) $sale->total, 2) }} {{ $sale->currency_code }}</td></tr></tfoot></table></div>
        <flux:callout variant="warning" icon="information-circle">{{ __('This is Local/Dev evidence only. Tax, discount, payment split, printer, and Production receipt policies remain PENDING.') }}</flux:callout>
    </div>
</x-layouts::app>
