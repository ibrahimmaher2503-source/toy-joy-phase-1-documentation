<!doctype html>
<html lang="{{ str_replace('_', '-', app()->getLocale()) }}" dir="{{ app()->getLocale() === 'ar' ? 'rtl' : 'ltr' }}"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><title>{{ __('Sale') }} {{ $sale->document_number }}</title>@vite(['resources/css/app.css'])</head><body class="bg-white p-8 text-zinc-900"><main class="mx-auto max-w-3xl"><header class="mb-8 flex items-start justify-between border-b border-zinc-300 pb-4"><div><h1 class="text-2xl font-bold">TOY &amp; JOY — {{ __('Retail Sale') }}</h1><p class="text-sm text-zinc-600">{{ __('Local/Dev print baseline — Production print policy PENDING') }}</p></div><div class="text-end"><div class="font-mono font-bold">{{ $sale->document_number }}</div><div class="text-sm">{{ $sale->approved_at?->format('Y-m-d H:i') }}</div></div></header><table class="w-full text-sm"><thead><tr class="border-b border-zinc-300"><th class="py-2 text-start">{{ __('Product') }}</th><th class="py-2 text-end">{{ __('Quantity') }}</th><th class="py-2 text-end">{{ __('Unit price') }}</th><th class="py-2 text-end">{{ __('Amount') }}</th></tr></thead><tbody>@foreach ($sale->lines as $line)<tr class="border-b border-zinc-100"><td class="py-3">{{ app()->getLocale() === 'ar' ? $line->name_ar : $line->name_en }}<div class="text-xs text-zinc-500">{{ $line->item_code }}</div></td><td class="py-3 text-end">{{ $line->quantity }}</td><td class="py-3 text-end">{{ number_format((float) $line->unit_price, 2) }}</td><td class="py-3 text-end">{{ number_format((float) $line->net_amount, 2) }}</td></tr>@endforeach</tbody><tfoot><tr><td colspan="3" class="pt-5 text-end font-bold">{{ __('Total') }}</td><td class="pt-5 text-end text-lg font-bold">{{ number_format((float) $sale->total, 2) }} {{ $sale->currency_code }}</td></tr></tfoot></table><footer class="mt-10 border-t border-zinc-300 pt-4 text-xs text-zinc-500">{{ __('Local/Dev only. Not an official Production receipt.') }}</footer></main><script>window.addEventListener('load', () => window.print());</script></body></html>
