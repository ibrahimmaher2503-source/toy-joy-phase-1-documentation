<x-layouts::app :title="__('Customer Import')">
<x-app.page :title="__('Customer Import')" :description="__('Upload, validate, review errors, and obtain independent approval before importing customers.')" max-width="7xl" class="space-y-6">
 <x-slot:actions><flux:button href="{{ route('customers.index') }}" variant="subtle" wire:navigate>{{ __('Back to customers') }}</flux:button></x-slot:actions>
 <flux:callout variant="info" title="{{ __('Reviewed customer import') }}"><p>{{ __('Consent is required and duplicates are warned or rejected. No automatic merge is performed. The requester cannot approve their own batch.') }}</p><flux:button href="{{ route('customers.import.template') }}" variant="subtle" icon="arrow-down-tray">{{ __('Download Excel template') }}</flux:button></flux:callout>
 <flux:card><flux:heading size="lg">{{ __('Upload and validate') }}</flux:heading><flux:text class="mt-2">{{ __('Excel, CSV, and ODS staging validates stable headers, required Arabic identity, consent policy fields, and duplicate phones before any customer is written.') }}</flux:text><form method="POST" action="{{ route('customers.import.stage') }}" enctype="multipart/form-data" class="mt-4 space-y-4">@csrf<flux:input type="file" name="import_file" accept=".xlsx,.csv,.ods" required /><flux:select name="mode"><flux:select.option value="create_only">{{ __('Create Only') }}</flux:select.option>@can('customers.edit')<flux:select.option value="update_existing">{{ __('Update Existing') }}</flux:select.option>@endcan</flux:select><flux:button type="submit" variant="primary">{{ __('Upload and validate') }}</flux:button></form>@error('import_file')<flux:text class="mt-2 text-red-600">{{ $message }}</flux:text>@enderror@if(session('success'))<flux:callout class="mt-4" variant="success">{{ session('success') }}</flux:callout>@endif</flux:card>
 <flux:card><flux:heading size="lg">{{ __('Staged batches and row errors') }}</flux:heading><div class="mt-4 overflow-x-auto"><flux:table><flux:table.columns><flux:table.column>{{ __('File') }}</flux:table.column><flux:table.column>{{ __('Status') }}</flux:table.column><flux:table.column>{{ __('Rows') }}</flux:table.column><flux:table.column>{{ __('Errors') }}</flux:table.column><flux:table.column>{{ __('Approval') }}</flux:table.column></flux:table.columns><flux:table.rows>@forelse($batches as $batch)<flux:table.row><flux:table.cell>{{ $batch->original_filename }}</flux:table.cell><flux:table.cell>{{ $batch->status }}</flux:table.cell><flux:table.cell>{{ $batch->valid_rows }} / {{ $batch->total_rows }}</flux:table.cell><flux:table.cell>{{ $batch->invalid_rows }}</flux:table.cell><flux:table.cell>@can('customers.import.approve') @if($batch->status==='ready_for_review' && $batch->invalid_rows===0 && $batch->created_by !== auth()->id())<form method="POST" action="{{ route('customers.import.approve',$batch) }}">@csrf<flux:button type="submit" size="sm" variant="primary">{{ __('Approve and import') }}</flux:button></form>@endif @endcan</flux:table.cell></flux:table.row>@empty<flux:table.row><flux:table.cell colspan="5">{{ __('No staged customer imports yet.') }}</flux:table.cell></flux:table.row>@endforelse</flux:table.rows></flux:table></div></flux:card>
</x-app.page>
</x-layouts::app>
